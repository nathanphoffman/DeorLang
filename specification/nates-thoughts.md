# Syntax.md
- Statements are not allowed to be newlined like this, this all seems wrong.  This is all very anti-deor, I want to 
enforce that all objects be instantiated using one line (hello,world)  if its too long that is an indication to nest a struct in a struct.  And your function should only take three parameters via enforced practices anyhow (make sure that is specified there)
- What is the deps keyword? is that part of our spec?

# Functions
- Shouldnt this be a contine? # skip — continue to next iteration
- Mention that there are no closures because there are no nested functions -- assuming that is true
- Shapes - Func Shapes link is broken, it should be docs/

# Deor vs Rust block
- We need to make this more obvious I think I would prefer two blocks one labeled Deor: the other Rust: I hate them sharing the same block in the docs.



