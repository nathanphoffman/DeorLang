 Foundational — needed for almost any real program:
  - Operators — never formally listed. We use +, -, *, / in examples but % (modulo), != (or is it not =?), integer division are all undefined
  - String operations — concatenation, length, interpolation. You can't write meaningful programs without these
  - Type conversion — int to float, float to int, int to string. No casting syntax defined
  - Void functions — fn main() has no return type in the sample but it's never defined. What do you write for functions that return nothing?
  - Built-ins — print, range, rand, floor, sqrt appear in examples but live nowhere in the spec. Are these deor: stdlib? Global built-ins?

  List gaps:
  - Index read — scores[0] for writing is shown, but reading by index is never defined
  - List length — no syntax for getting the count of elements
  - remove collapse — just discussed, should add explicit note to docs

  Language mechanics not defined:
  - Module system — how do you import your own .deor files? What does (something) in utils resolve to — a file called utils.deor?
  - Scope and shadowing — variable scope rules within blocks, whether shadowing is allowed 
  - Nested functions — mentioned as allowed, never formally shown
  - Comments — # is used in examples, never specced
  - Numeric literals — underscores (1_000_000)? Hex (0xFF)? Float suffixes?
  - Truthiness beyond validator types — can you do if my_list to check non-empty? if my_string?

  Still in open-questions.md (from before):
  - Pattern matching / match
  - Error handling (Result-like for recoverable errors)
  - String semantics (&str vs String inference)
  - User-defined generics

  Transpiler improvements:
  - Error on missing return type — if the token after the return type in fn decl isn't an IDENT (e.g. fn main() with no void), print an error and exit instead of generating garbage Rust. Add exit_err(string message) to rust_helpers.deor and validate in gen_fn_decl.

  Spec features not yet implemented in the transpiler:
  - is not — two-word inequality operator (transpiler may silently misparse x is not y)
  - Compound assignment — +=, -=, *=, /=, %= all in the spec, none in the transpiler
  - throw — throw msg should transpile to panic!(); not implemented
  - with — struct record update (old_room with (new_area)); not implemented
  - remove at — list element removal by index; not implemented
  - List slices — rooms in range(start, end) extracting a sublist; not implemented
  - const — immutable top-level primitive bindings; not implemented
  - Validator types — type declarations, Option<T> semantics, avow, else coalescing; not implemented
  - as for list construction — rooms as [a, b, c] (type inferred from items); not implemented
  - Named-args enforcement — no inline literals in user-defined function calls; not enforced
  - private visibility modifier — not implemented

  Discussed this session, not written yet:
  - First-class functions / function type parameter syntax
  - No built-in filter/map/reduce (the opinionated decision should be noted somewhere)
  - remove shrinks dynamic lists (explicit doc note)
