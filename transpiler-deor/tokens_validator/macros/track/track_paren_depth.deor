
# Tracks LPAREN/RPAREN nesting depth so other macros can distinguish top-level tokens from those inside expressions.
macro track_paren_depth
	if cur_kind is "LPAREN"
		paren_depth = paren_depth + 1
	if cur_kind is "RPAREN"
		paren_depth = paren_depth - 1
